<Window
    x:Class="AirPodsBattery.FlyoutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:AirPodsBattery.Controls">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="20,16,20,14" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header: model name + live/stale dot -->
        <Grid Grid.Row="0">
            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE7F6;" FontSize="16" Opacity="0.85" />
                <TextBlock x:Name="ModelText" Text="AirPods" FontSize="14" FontWeight="SemiBold" />
            </StackPanel>
            <Ellipse
                x:Name="StatusDot"
                Width="8" Height="8"
                HorizontalAlignment="Right" VerticalAlignment="Center"
                Fill="#9CA3AF" />
        </Grid>

        <!-- Hero gauge (AirPods Max / no reading yet) -->
        <Viewbox x:Name="HeroBox" Grid.Row="1" Height="176" VerticalAlignment="Center">
            <controls:BatteryGauge x:Name="HeroGauge" />
        </Viewbox>

        <!-- Per-component gauges (in-ear models) -->
        <StackPanel
            x:Name="BudsPanel"
            Grid.Row="1"
            Orientation="Horizontal"
            Spacing="18"
            HorizontalAlignment="Center" VerticalAlignment="Center"
            Visibility="Collapsed">
            <controls:BatteryGauge x:Name="LeftGauge" />
            <controls:BatteryGauge x:Name="RightGauge" />
            <controls:BatteryGauge x:Name="CaseGauge" />
        </StackPanel>

        <!-- Preferred output device + one-click connect & switch -->
        <Grid Grid.Row="2" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ComboBox
                x:Name="DeviceCombo"
                HorizontalAlignment="Stretch"
                PlaceholderText="Output device…"
                SelectionChanged="DeviceCombo_SelectionChanged" />
            <Button
                x:Name="SyncButton"
                Grid.Column="1"
                Style="{StaticResource AccentButtonStyle}"
                Click="SyncButton_Click">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE895;" FontSize="14" />
                    <TextBlock Text="Sync" />
                </StackPanel>
            </Button>
        </Grid>

        <TextBlock
            x:Name="StatusText"
            Grid.Row="3"
            FontSize="12"
            Opacity="0.6"
            TextWrapping="Wrap"
            Text="Listening for Bluetooth broadcasts…" />

        <TextBlock
            x:Name="SystemText"
            Grid.Row="4"
            FontSize="11"
            Opacity="0.45"
            TextWrapping="Wrap"
            Text=" " />
    </Grid>
</Window>
